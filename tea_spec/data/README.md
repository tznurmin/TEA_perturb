# Vendored data

This repository vendors the UniProt organism list used to build a species-name whitelist.

Files:

- `speclist.txt` — UniProt organism list (scientific names, synonyms, taxonomy IDs)
- `attribution.txt` — required attribution and licensing notes for the vendored file

The source material is included for reproducibility. See `attribution.txt` for formal details.
